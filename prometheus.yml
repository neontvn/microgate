# Prometheus config — scrapes the gateway's /metrics endpoint every 5 seconds.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "gateway"
    static_configs:
      # host.docker.internal lets Docker containers reach host's localhost
      - targets: ["host.docker.internal:8080"]
